# Resend credentials and contact routing. Keep real values in a local .env
# file and configure them in the Netlify dashboard for deployed functions.
RESEND_API_KEY=
CONTACT_EMAIL=
CONTACT_FROM_EMAIL=
